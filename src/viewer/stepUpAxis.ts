import type { ForwardAxis, UpAxis } from './types';

// Approved STEP default shared by native and WebAssembly imports.
export const stepUpAxis: UpAxis = 'y';
export const stepForwardAxis: ForwardAxis = 'z';
